import type { Metadata, Viewport } from "next";
import "./globals.css";

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  viewportFit: "cover",
};

export const metadata: Metadata = {
  title: "Athena — AI Learning Platform",
  description: "AI-powered visual learning for Indian students. Ask anything, learn with animations and voice.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="min-h-screen" style={{background: '#000'}}>
        {children}
      </body>
    </html>
  );
}
